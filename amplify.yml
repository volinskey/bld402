version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cp -r src public/src
    build:
      commands:
        - echo "Static site — no build step needed"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
