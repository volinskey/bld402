{
  "name": "bld402",
  "version": "0.1.0",
  "description": "Build web apps without code on run402 — an AI agent accessibility layer",
  "private": true,
  "scripts": {
    "start": "npx serve public -l 3000"
  },
  "dependencies": {
    "@run402/sdk": "^2.2.0",
    "@x402/core": "^2.12.0",
    "@x402/evm": "^2.12.0",
    "@x402/fetch": "^2.12.0",
    "sharp": "^0.34.5",
    "viem": "^2.47.0"
  }
}
