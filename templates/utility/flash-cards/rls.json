{
  "policies": [
    {
      "template": "user_owns_rows",
      "tables": [
        { "table": "decks", "owner_column": "user_id" },
        { "table": "progress", "owner_column": "user_id" }
      ]
    },
    {
      "template": "public_read",
      "tables": [
        { "table": "cards" }
      ]
    }
  ],
  "notes": "user_owns_rows on decks and progress (users see only their own). Cards are public_read since they belong to decks — access is gated by the deck being visible. Public decks need a custom read policy: SELECT allowed WHERE decks.is_public = true (agent should add this manually via SQL after applying the template)."
}
